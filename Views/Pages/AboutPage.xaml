<Page x:Class="StreamCapturePro.Views.Pages.AboutPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:viewmodels="clr-namespace:StreamCapturePro.ViewModels"
      d:DataContext="{d:DesignInstance Type=viewmodels:AboutViewModel}"
      mc:Ignorable="d" 
      d:DesignHeight="550" d:DesignWidth="700"
      Title="AboutPage">

    <Grid Margin="16,12,16,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 核心功能 -->
        <ui:Card Grid.Row="1" Padding="16,12" Margin="0,0,0,12" VerticalAlignment="Top">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <ui:SymbolIcon Symbol="Info24" FontSize="18" Margin="0,0,8,0" Foreground="{DynamicResource SystemAccentColorPrimaryBrush}"/>
                    <TextBlock Text="关于本工具" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock TextWrapping="Wrap" LineHeight="20" FontSize="13" Foreground="{DynamicResource TextFillColorPrimaryBrush}">
                    <Run Text="推流码获取工具 (StreamCapture) 是一款专为开发者与学习者设计的本地推流信息辅助提取工具。" />
                    <LineBreak/>
                    <Run Text="用于帮助用户在本地环境快速提取目标直播软件的推流地址 (Server) 和串流密钥 (Key)，以用于 OBS 等第三方软件的本地测试推流。" />
                </TextBlock>
            </StackPanel>
        </ui:Card>

        <!-- 免责声明 -->
        <ui:Card Grid.Row="2" Padding="16,12" VerticalAlignment="Top">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <ui:SymbolIcon Symbol="Warning24" FontSize="18" Margin="0,0,8,0" Foreground="{DynamicResource SystemAccentColorPrimaryBrush}"/>
                    <TextBlock Text="免责声明" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
                
                <TextBlock TextWrapping="Wrap" LineHeight="22" FontSize="12.5" Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                    <Run FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}">1. 仅限个人使用：</Run>
                    <Run Text="本软件仅用于个人学习和测试使用，无需提供任何代价，并不可用于任何商业用途及目的。"/>
                    <LineBreak/><LineBreak/>
                    <Run FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}">2. 遵守平台规范：</Run>
                    <Run Text="使用者需严格遵守各大直播平台方的服务协议与管理规定。若平台方禁止使用第三方工具或非官方推流手段，请立即停止使用并删除本工具。"/>
                    <LineBreak/><LineBreak/>
                    <Run FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}">3. 风险自担：</Run>
                    <Run Text="作者不对因使用本工具而导致的任何账号封禁、数据丢失或法律纠纷承担任何责任。使用本工具即代表您完全理解并同意本声明。"/>
                </TextBlock>
            </StackPanel>
        </ui:Card>
    </Grid>
</Page>