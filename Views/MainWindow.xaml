<ui:FluentWindow x:Class="StreamCapturePro.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        mc:Ignorable="d"
        Title="推流码获取工具" Height="600" Width="720"
        Icon="pack://application:,,,/assets/logo.ico"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="推流码获取工具">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/assets/logo.ico" Width="16" Height="16" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <ContentControl Grid.Row="1" x:Name="PageHost" />

        <tb:TaskbarIcon x:Name="TrayIcon"
                        Grid.Row="0"
                        IconSource="pack://application:,,,/assets/logo.ico"
                        ToolTipText="推流码获取工具运行中">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="打开主面板" Click="TrayOpen_Click"/>
                    <Separator />
                    <MenuItem Header="退出程序" Click="TrayExit_Click"/>
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>

        <ui:SnackbarPresenter x:Name="RootSnackbar" Grid.Row="1" VerticalAlignment="Bottom" HorizontalAlignment="Center"/>
    </Grid>
</ui:FluentWindow>
